# Core -- the package itself imports only these.
numpy>=1.24,<3
scipy>=1.10
pandas>=2.0

# Speed. Without numba everything still runs, ~250x slower.
# Numba tracks CPython releases with a lag: use Python 3.11 or 3.12.
numba>=0.59
joblib>=1.3

# Figures.
matplotlib>=3.7

# Dataset fetching (scripts/verify_corpus.py only).
ucimlrepo>=0.0.7

# TEST-ONLY. No module under colreg/ imports scikit-learn; it exists solely as
# the reference implementation the parity suite checks against. That separation
# is what makes the parity claim meaningful -- keep it.
scikit-learn>=1.3
pytest>=7.4
